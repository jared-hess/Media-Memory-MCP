app:
  data_dir: /tmp/media-memory-cli-fixture
  corpus_id: local
embeddings:
  provider: mock
  model: mock
  dimensions: 8
index:
  sqlite_path: /tmp/media-memory-cli-fixture/media-memory.sqlite
  vector_path: /tmp/media-memory-cli-fixture/vectors
media_sources:
  - type: filesystem
    enabled: true
    name: fixture-media
    roots:
      - tests/fixtures/media
    read_only: true
    extensions:
      - .mkv
      - .mp4
  - type: plex
    enabled: false
subtitle_sources:
  opensubtitles:
    enabled: false
  bazarr:
    enabled: false
search:
  default_limit: 5
  max_limit: 25
